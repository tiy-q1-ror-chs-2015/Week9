= form_for @quote do |f|
  - if @quote.errors.any?
    #error_explanation
      %h2= "#{pluralize(@quote.errors.count, "error")} prohibited this quote from being saved:"
      %ul
        - @quote.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :quote
    = f.text_area :quote
  .field
    = f.label :quote_master
    = f.text_area :quote_master
  .actions
    = f.submit 'Save'
