%h1 Listing quotes

%table
  %thead
    %tr
      %th Quote
      %th Quote master
      %th
      %th
      %th
      
  %tbody
    - @quotes.each do |quote|
      %tr
        %td= quote.quote
        %td= quote.quote_master
        %td= link_to 'Show', quote
        %td= link_to 'Edit', edit_quote_path(quote)
        %td= link_to 'Destroy', quote, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Quote', new_quote_path
